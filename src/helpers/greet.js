/**
 * A simple helper that returns a greeting message.
 * Used to test that PKG.import() can resolve local .js files.
 */
export function greet(name) {
  return `Hello, ${name}! Welcome to SomMark.`;
}

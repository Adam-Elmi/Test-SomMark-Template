# sommark-template

A template for new SomMark web projects.

To install dependencies:

```bash
bun install
```
---

To run the dev server:

```bash
bun run dev
```

---

To build the project:

```bash
bun run build
```

---

To run the preview server:

```bash
bun run preview
```

